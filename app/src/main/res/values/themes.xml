<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.EndProject" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/simpleBlue_main</item>
        <item name="colorPrimaryVariant">@color/simpleBlue_main</item>
        <item name="colorOnPrimary">@color/background_light</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.EndProject.Splash" parent="Theme.SplashScreen">
        <!-- 배경 색 -->
        <item name="windowSplashScreenBackground">@color/white</item>

        <!-- 로고 이미지 추가 -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_logo</item>

        <!-- 배경 원 없애기 (선택) -->
        <item name="windowSplashScreenIconBackgroundColor">@android:color/transparent</item>

        <!-- 스플래시 끝난 뒤 적용할 테마 -->
        <item name="postSplashScreenTheme">@style/Theme.EndProject</item>
    </style>

    <style name="Theme.EndProject.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="Theme.EndProject.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.EndProject.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>